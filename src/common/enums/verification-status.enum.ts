export enum VerificationStatus {
  INITIATED = 'initiated',
  PENDING = 'pending',
  IN_REVIEW = 'in_review',
  APPROVED = 'approved',
  REJECTED = 'rejected',
  RESUBMIT_REQUIRED = 'resubmit_required',
  EXPIRED = 'expired',
}
